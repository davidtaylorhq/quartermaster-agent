{
  "name": "quartermaster-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types --no-warnings=ExperimentalWarning 'bin/*.test.ts' 'lib/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "js-yaml": "^4.1.0",
    "typescript": "^5.7.0"
  }
}
